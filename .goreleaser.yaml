version: 2

builds:
  - id: timon-mcp
    main: ./cmd/timon-mcp
    binary: timon-mcp
    env:
      - CGO_ENABLED=0
    goos:
      - darwin
      - linux
      - windows
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X github.com/FlowmaxAITrade/timon-mcp/internal/version.Version={{ .Version }}
      - -X github.com/FlowmaxAITrade/timon-mcp/internal/version.Commit={{ .Commit }}
      - -X github.com/FlowmaxAITrade/timon-mcp/internal/version.BuildDate={{ .Date }}

archives:
  - format: tar.gz
    format_overrides:
      - goos: windows
        format: zip
    name_template: "{{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}"

checksum:
  name_template: "checksums.txt"

# release-please owns the release (tag + changelog); GoReleaser only uploads
# binaries to the existing release instead of creating a duplicate one.
release:
  mode: keep-existing
